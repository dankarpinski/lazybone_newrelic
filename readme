pybluez:
sudo apt-get install bluez
sudo apt-get install python-bluez python-gobject libbluetooth-dev python-dev
hcitool dev

install dependencies:
- ideally with virtualenv[https://virtualenv.pypa.io/en/latest/installation.html]
- pip install -r requirements.txt

pairing: sudo bluez-simple-agent hci0 30:14:10:27:17:25

test bluetooth pairing:
python app.py test

set newrelic rss event feed in app.py addr var.
python app.py run
